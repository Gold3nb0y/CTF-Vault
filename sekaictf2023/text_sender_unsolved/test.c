#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int main(void){
    char* chef = malloc(0x10);
    getline(&chef, &size, stdin);
    printf("%ld\n",lol);

    
    return 0;
}
